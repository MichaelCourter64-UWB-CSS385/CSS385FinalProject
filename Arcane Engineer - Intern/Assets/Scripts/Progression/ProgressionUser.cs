// Used to essentially tag scripts that use the progression system. 
// There's a text file of every class that inherits from this.
// You can also use Visual Studio's Find tool with the entire project 
// setting to find uses of the interface.
public interface ProgressionUser { }
